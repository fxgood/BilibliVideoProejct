<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD MAPPER 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njust.bilibili.dao.UserDao">
    <select id="getUserByPhone" parameterType="java.lang.String" resultType="com.njust.bilibili.domain.User">
        select *
        from t_user
        where phone = #{phone}
    </select>
    <select id="getUserById" parameterType="java.lang.Long" resultType="com.njust.bilibili.domain.User">
        select *
        from t_user
        where id = #{id}
    </select>
    <select id="getUserInfoByUserId" parameterType="java.lang.Long" resultType="com.njust.bilibili.domain.UserInfo">
        select *
        from t_user_info
        where userid = #{userId}
    </select>
    <insert id="addUser" parameterType="com.njust.bilibili.domain.User" useGeneratedKeys="true"
            keyProperty="id"><!--这俩参数可以返回主键-->
        insert into t_user(phone,
                           email,
                           password,
                           salt,
                           createTime)
        values (#{phone},
                #{email},
                #{password},
                #{salt},
                #{createTime})
    </insert>

    <insert id="addUserInfo" parameterType="com.njust.bilibili.domain.UserInfo">
        insert into t_user_info(userid,
                                nick,
                                avatar,
                                sign,
                                gender,
                                birth,
                                createTime)
        values (#{userid},
                #{nick},
                #{avatar},
                #{sign},
                #{gender},
                #{birth},
                #{createTime})
    </insert>

    <update id="updateUsers" parameterType="com.njust.bilibili.domain.User">
        update t_user
        set
        <if test="phone!=null and phone !=''">
            phone=#{phone},
        </if>
        <if test="email!=null and email !=''">
            email=#{email},
        </if>
        <if test="password!=null and password!=''">
            password=#{password},
        </if>
        <if test="salt!=null and salt!=''">
            salt=#{salt},
        </if>
        updateTime=#{updateTime}
        where
        id=#{id}
    </update>

    <select id="getUserByEmail" parameterType="java.lang.String" resultType="com.njust.bilibili.domain.User">
        select *
        from t_user
        where email = #{email}
    </select>
    <update id="updateUserInfos" parameterType="com.njust.bilibili.domain.UserInfo">
        update t_user_info
        set
        <if test="nick!=null and nick!=''">
            nick=#{nick},
        </if>
        <if test="avatar!=null and avatar!=''">
            avatar=#{avatar},
        </if>
        <if test="sign!=null and sign!=''">
            sign=#{sign},
        </if>
        <if test="gender!=null and gender!=''">
            gender=#{gender},
        </if>
        <if test="birth!=null and birth!=''">
            birth=#{birth},
        </if>
            updateTime=#{updateTime}
        where
            userid=#{userid}
    </update>

    <select id="getUserInfoByUserIds" resultType="com.njust.bilibili.domain.UserInfo">
        select *
        from t_user_info
        where 1=1
            <if test="userIdList!=null and userIdList.size>0">
                and userId in
                    <foreach collection="userIdList" item="userId" index="index" open="(" separator="," close=")">
                        #{userId}
                    </foreach>
            </if>
    </select>

    <select id="pageCountUserInfos" parameterType="java.util.Map" resultType="java.lang.Integer">
        select count(1)
        from t_user_info
        where 1=1
            <if test="nick!=null and nick!='' ">
                and nick like '%${nick}%'
            </if>
    </select>

    <select id="getListUserInfos" parameterType="java.util.Map" resultType="com.njust.bilibili.domain.UserInfo">
        select *
        from t_user_info
        where
            1=1
            <if test="nick!=null and nick!=''">
                and nick like '%${nick}%'
            </if>
        order by id asc
        limit #{start},#{limit}
    </select>
</mapper>